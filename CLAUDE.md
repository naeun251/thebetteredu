# 과외홈페이지 (betteredu.kr)

더나은에듀 과외 홈페이지 및 블로그. GitHub 저장소(naeun251/thebetteredu)에 push하면
GitHub Pages로 자동 배포됨. 노트북과 집 컴퓨터를 오가며 작업하므로, 항상 git을 통해
동기화한다 (OneDrive 동기화는 사용하지 않음 — 과거 동기화 문제로 폴더를 옮긴 적 있음).

## 작업 규칙 (매 세션 필수)

1. **작업이 끝나면 항상 git add + commit + push까지 완료한다.** 커밋만 하고 push를
   안 하면 다른 컴퓨터에서 안 보인다. 세션 중간이라도 의미 있는 단위 작업이 끝나면
   바로 push하는 것을 기본으로 한다.
2. **다른 컴퓨터로 넘어가기 전에는 꼭 push 여부를 확인**하고, 넘어간 컴퓨터에서는
   작업 시작 전에 `git pull origin master`로 최신 상태를 받는다.
3. **이 파일(CLAUDE.md) 아래 "작업 로그"를 세션 종료 시 갱신한다.** 새 항목을
   맨 위에 추가하고, 날짜 + 무엇을 했는지 + 다음에 이어할 것을 짧게 남긴다. 이렇게
   하면 노트북/집 컴퓨터 어느 쪽에서 새로 시작해도 이 파일만 읽으면 맥락을 알 수 있다.
4. 실제 학교(중학교/고등학교) 관련 구체적 정보(주소, 학생 수 등)를 지어내지 않는다 —
   실존 기관에 대한 사실이므로, 사용자가 제공하기 전까지는 placeholder로 남겨둔다.

## 사이트 구조

- `index.html` — 홈페이지 (지역 선택 → 구별 카드)
- `blog/index.html` — 블로그 목록 페이지 (동일한 지역 선택 위젯 포함, index.html과
  구조가 거의 같지만 별도 파일이라 지역 카드(districtPosts)를 양쪽 다 수정해야 함)
- `blog/{slug}-gwaoe.html` — 구별 **중립 허브 페이지** (과목 무관, 동네·학교 안내 +
  과목별 안내). 홈페이지 지역 카드가 이 페이지로 연결됨.
- `blog/{slug}-math-gwaoe.html` — 구별 **수학 전문** 페이지 (기존 콘텐츠, 실제 학교
  기출 포인트 등 상세 내용 포함). 허브 페이지의 "수학 과외 보기" 링크가 여기로 감.
- `blog/{gu}-{dong}-gwaoe.html` — 동네별 페이지 (역시 지금은 수학 전문)
- `blog/{school}-ms-gwaoe.html` / `-gms-gwaoe.html` — 학교별 페이지. 상단에 "학교
  정보" placeholder 박스 있음 (실제 정보 받으면 채울 것)
- `sitemap.xml` — 네이버/구글 검색 노출을 위해 blog/ 안의 모든 페이지가 등록되어
  있어야 함. 새 블로그 파일을 추가하면 sitemap.xml도 같이 갱신할 것.
- `blog/images/서울중등/{구}.jpg` — 중학교 지역 카드 사진 (구 이름으로 정리됨). 원본
  파일명은 "과외(썸네일) 26~50.jpg" 형태의 번호 파일이며, 26=도봉구, 27=노원구,
  28=강북구, 29=은평구, 30=종로구, 31=성북구, 32=중랑구, 33=마포구, 34=중구,
  35=성동구, 36=광진구, 37=강서구, 38=양천구, 39=용산구, 40=강남구, 41=송파구,
  42=구로구, 43=동작구, 44=서초구, 45=강동구, 46=금천구, 47=관악구, 48=서대문구,
  49=동대문구, 50=영등포구 순으로 매핑됨(이미지에 구 이름이 텍스트로 박혀있어
  확인 가능) — 향후 같은 번호 체계의 새 이미지를 받으면 이 매핑을 재사용하면 됨.
- `blog/images/서울고등/{구}.jpg` — 고등학교 지역 카드 사진 (구 이름으로 정리됨).
  현재 도봉구·노원구·강북구·은평구·종로구 5개 구만 존재(고등학교 콘텐츠가 이
  5개 구에만 있음). 번호→구 매핑은 서울중등과 동일한 체계 사용.
  (2026-07-18: 기존 "서울중등전문"/"서울고등전문" 폴더는 삭제되고 이 폴더들로
  교체됨 — 폴더명에서 "전문"이 빠진 것에 주의)

## 구 단위 허브 페이지 패턴 (25개 구 전부 적용됨)

각 허브 페이지는:
1. 동네별 학교 안내 (배지 클릭 → 인라인으로 "수학/영어/국어 과외 중 선택" 패널이
   그 자리에 펼쳐짐, 새 페이지로 안 넘어감. 수학만 실제 콘텐츠 있고 나머지는 "준비중")
2. 과목별 과외 안내 카드 (수학=링크 있음, 영어/국어=준비중)
3. 과외 선택 시 고려할 점

생성용 스크립트는 세션 중 임시로 스크래치 폴더에 작성했었고 저장소에는 없음 — 비슷한
작업(예: 고등학교 버전) 필요시 처음부터 다시 접근 방식을 설계해야 함. 특히 고등학교는
서울시 학군이 중학교처럼 동네 단위가 아니라 더 넓은 학군 단위로 배정되는 경우가 많아서,
중학교와 동일한 "동네별 학교" 구조가 그대로 맞지 않을 수 있음 — 실제 데이터 확인 필요.

## 작업 로그

### 2026-07-20
- 고등학교용 블로그 콘텐츠를 5개 구 추가 제작: 영등포·용산·강남·송파·구로. 사용자가
  동네별 고등학교 목록을 줘서, 기존 15개 구 패턴(허브+동네+학교 3단 구조, 어제
  세션의 Node.js 스크립트를 재사용해 CSS·nav·footer를 문자열 그대로 재사용하는
  방식) 그대로 적용해 총 91개 페이지 생성 (허브 5 + 동네 33 + 학교 53).
- **같은 학교가 목록에서 서로 다른 동에 중복으로 나온 경우 3건**을 생성 전에
  사용자에게 확인받음(학교 페이지는 동 하나에만 연결되는 구조라서 임의로 고를 수
  없었음): 구로구 고척고등학교(개봉동/고척동 중복 → 고척동으로 확정), 송파구
  오금고등학교(마천동/오금동 중복 → 오금동으로 확정, 마천동은 학교가 없어져
  동네 자체를 제외), 송파구 잠실여자고등학교(석촌동/잠실동 중복 → 사용자가
  둘 다 아닌 **송파동**으로 지정, 석촌동도 학교가 없어져 제외). **다음에도 같은
  학교 이름이 서로 다른 동에 두 번 나오면 절대 임의로 하나를 고르지 말고 반드시
  사용자에게 확인할 것** — 실존 학교의 소재지 정보라 추측하면 안 됨.
- **학교 슬러그가 이미 다른 구에 존재하는 경우 2건**도 생성 전 발견해 사용자에게
  확인함: 한성과학고등학교(이미 서대문구 현저동에 있는데 이번 목록엔 영등포구
  여의도동으로 나옴), 광성고등학교(이미 마포구 신수동에 있는데 이번 목록엔 송파구
  풍납동으로 나옴) — 둘 다 사용자가 "기존 유지"를 선택해 새 목록에서는 제외하고
  기존 페이지를 그대로 둠. **매 배치 작업 전에 새로 만들 학교 슬러그 전체를
  기존 blog/ 폴더 파일명과 대조하는 사전 체크를 항상 먼저 돌릴 것** — 이번에
  스크립트로 미리 grep 대조했더니 정확히 이 2건만 걸러졌고, 덕분에 기존 페이지를
  덮어쓰는 사고를 막음.
- 새 학교 슬러그 규칙 추가 사례: "단국대학교부속소프트웨어고등학교"처럼 "OO대학교
  부속"(사범대학 없이) 구조는 기존 "OOdaebusok"(사범대학부속) 표기와 구분해
  "OObusok"으로 표기(dangukbusoksoftware). "소프트웨어"는 특수목적 유형 키워드
  목록(과학/예술/디자인/외국어/국제/관광/미디어/IT)에 문자 그대로는 없지만 IT
  계열로 보고 "go" 생략 적용. "한림연예예술고등학교"는 "예술" 키워드로 go 생략
  (hallimyeonyeyesul). 이 두 케이스처럼 특수목적 키워드가 명시적으로 없어도
  성격상 동일 계열이면 유추 적용 — 다음에도 비슷한 신조어 학교명 나오면 이 판단
  기준 재사용.
- index.html·blog/index.html에 5개 구 고등학교 카드 추가할 때, **어제 겪었던
  "중복 닫는 괄호" 버그를 이번엔 사전에 인지하고 스크립트를 수정**(기존 `}] },`
  줄 뒤에 새 블록을 삽입하는 대신, 그 줄 자체를 새 블록으로 통째로 교체하도록
  변경)해서 이번엔 처음부터 구문 오류 없이 성공. `new Function()`으로
  districtPosts 파싱 검증도 정상.
- sitemap.xml에 91개 URL 전부 등록. 등록 후 실제 파일 수(343)와 sitemap 내
  hs-gwaoe URL 수(343)가 정확히 일치.
- 이제 20개 구 완료(도봉/노원/강북/은평/종로/성북/중랑/서대문/마포/중구/동대문/
  성동/광진/강서/양천/영등포/용산/강남/송파/구로), 나머지 5개 구(강동/금천/관악/
  서초/동작)는 아직 고등학교 콘텐츠 없음 — 사용자가 동네별 고등학교 목록을 주면
  이어서 진행.
- 아직 안 한 것: git add/commit/push (사용자 확인 후 진행 예정).

### 2026-07-19
- 고등학교용 블로그 콘텐츠를 5개 구 추가 제작: 동대문·성동·광진·강서·양천. 사용자가
  동네별 고등학교 목록을 줘서, 기존 10개 구(도봉/노원/강북/은평/종로/성북/중랑/서대문/
  마포/중구) 패턴(허브+동네+학교 3단 구조)을 그대로 재사용해 총 67개 페이지 생성
  (허브 5 + 동네 23 + 학교 39). 이번엔 스크래치 폴더의 Node.js 스크립트를 저장소
  커밋 없이 그때그때 작성하는 기존 방식 대신, 기존 페이지 3종(hub/dong/school)의
  HTML을 통째로 읽어 CSS·nav·footer 보일러플레이트를 문자열 그대로 재사용하는
  방식으로 생성 — 스타일 시트가 페이지 한 글자도 안 틀리게 기존 파일들과 일치함.
- 동네 슬러그: 기존 중학교 페이지에 이미 있던 슬러그는 그대로 재사용(예: 성동구
  마장동→majangdong). 중학교 페이지가 없던 새 동네는 새로 슬러그 생성: 이문동→
  imundong, 금호동4가→geumhodong4ga, 군자동→gunjadong, 화양동→hwayangdong,
  개화동→gaehwadong. 강서구 "내발산동"이 사용자 목록에 두 번(명덕여자고등학교,
  수명고등학교) 나뉘어 나왔는데, 같은 동이므로 한 동네 블록으로 합쳐서 학교 2개를
  넣음 — 같은 동이 여러 줄로 쪼개져 들어오면 항상 합칠 것.
- 학교 슬러그는 기존 규칙(OO고등학교→OOgo, OO여자고등학교→OOyeoja no go, 특수목적
  유형 키워드(과학/예술/디자인/외국어/국제/관광/미디어/IT) 있으면 go 생략, OO대학교
  사범대학부속(여자)고등학교→OOdaebusok(yeoja)) 그대로 적용. "동국대학교사범대학
  부속고등학교"(동대문구 장안동, 남녀공학)와 이미 중구에 있던 "동국대학교사범대학
  부속여자고등학교"(donggukdaebusokyeoja, 중구 장충동2가)는 서로 다른 학교이므로
  슬러그 충돌 없음 — 이름이 비슷한 부속고 계열은 항상 여자/공학 여부와 소재 구를
  먼저 확인하고 슬러그를 정할 것.
- index.html·blog/index.html의 districtPosts 객체에 5개 구 고등학교 항목을 추가할
  때, 문자열 삽입 위치를 "기존 중학교 블록의 닫는 `}] },` 뒤"로 잡았다가 원래 있던
  닫는 줄을 안 지우고 새 `}], 고등학교: [{` 줄을 그 뒤에 추가해버려서 `}] },` 두 줄이
  겹치는 구문 오류가 5곳씩(양쪽 파일) 생겼었음 — 정규식으로 중복된
  `}] },\n}], 고등학교: [{` 패턴을 찾아 하나로 합쳐 수정, `new Function()`으로
  districtPosts 객체 리터럴 파싱 성공까지 확인 후 마무리. **다음에 같은 종류의 삽입
  작업을 할 때는 "뒤에 추가"가 아니라 기존 닫는 줄을 통째로 새 블록으로 교체하는
  방식을 쓸 것** (index.html은 8/6칸, blog/index.html은 6/4칸으로 두 파일의 들여쓰기
  칸수가 다르다는 것도 이번에 확인 — 하드코딩하지 말고 원본 줄에서 들여쓰기를
  추출해서 맞춰야 함).
- sitemap.xml에 67개 URL 전부 등록(허브 0.8, 동네 0.7, 학교 0.6 — 기존 고등학교
  URL과 동일한 우선순위 체계). 등록 후 실제 파일 수(252)와 sitemap 내 hs-gwaoe
  URL 수(252)가 정확히 일치하는 것으로 확인.
- git bash(Git Bash/MSYS)에서 heredoc이나 `node -e "..."`로 JS 정규식(`\\s`, `\\{`
  등)을 전달하면 백슬래시가 예상과 다르게 씹히는 문제를 겪음(`\\s` → `s`로 소실).
  **앞으로 이런 문자 이스케이프가 중요한 스크립트는 Bash heredoc/`-e` 대신 Write
  도구로 .js 파일을 직접 써서 node로 실행할 것** — 이번에 그렇게 바꾸고 나서야
  정규식이 의도대로 동작함.
- 남은 20개 구(강남·서초·송파·강동·양천 등 이미 된 것 제외 — 관악·구로·금천·
  동작·서초·성동(완료)·송파·양천(완료)·영등포·용산 등)는 아직 고등학교 콘텐츠
  없음. 이제 15개 구 완료(도봉/노원/강북/은평/종로/성북/중랑/서대문/마포/중구/
  동대문/성동/광진/강서/양천), 나머지 10개 구(강남/송파/구로/동작/서초/강동/
  금천/관악/영등포/용산)는 사용자가 동네별 고등학교 목록을 주면 이어서 진행.
- 아직 안 한 것: git add/commit/push (사용자 확인 후 진행 예정).

### 2026-07-18 (5)
- kids/index.html 커리큘럼 섹션을 "준비중" coming-box에서 실제 내용으로 교체:
  유아(한글/영어 파닉스/수학 사고력), 초등(국어/영어/수학/사회/과학) + 왜 이 시기에
  배워야 하는지 설명.
- 키즈 전문 방문수업이 가능한 지역이 정해져 있어서(사용자가 51개 지역 목록 제공),
  "1:1 과외 우리 동네 지역별 가이드"와 같은 목적(검색 노출용 지역 키워드 페이지)으로
  `blog/{지역}-kids-gwaoe.html` 51개를 신규 생성. 서울 18개 구는 기존에 이미 쓰던
  구 슬러그(gangnam, dobong 등)를 그대로 재사용, 나머지 33개(경기·경남·경북·광주·
  대구·부산·인천·충남)는 새로 슬러그를 만듦 — 북구/남구/중구처럼 여러 시/도에
  같은 구 이름이 있는 경우 도시명을 접두어로 붙여 구분(예: gwangju-bukgu,
  busan-bukgu). 페이지 구조는 서울 구 허브 페이지 스타일을 재사용하되, 학교/동네
  세부 안내 없이 커리큘럼 요약 + "왜 배워야 하는지" + 방문수업 안내 + CTA로 단순화
  (지역마다 학교 데이터가 없어서 3단 구조가 필요 없었음).
- kids/index.html에 "방문수업 가능 지역" 섹션 추가, 51개 지역을 시/도별로 그룹핑한
  칩 목록으로 각 지역 페이지에 링크. sitemap.xml에도 51개 URL 전부 등록.
- 지역 목록 원본: 경기(부천 원미구/안산 상록구/의정부/이천/평택), 경남(고성/김해/
  양산/창원 성산구/창원 의창구), 경북(경산/구미), 광주(광산/북), 대구(수성),
  부산(금정/남/동래/부산진/북/사상/수영/사하/연제/중/해운대/동), 서울(강남·강동·
  강북·강서·광진·노원·도봉·동대문·동작·서대문·서초·성동·성북·송파·영등포·은평·
  종로·중랑 18개), 인천(계양/남동/미추홀/부평), 충남(천안 동남구/천안 서북구).
  나중에 지역이 추가/제외되면 이 목록과 kids/index.html의 area-group, sitemap.xml을
  같이 갱신할 것.

### 2026-07-18 (4)
- 새 상단 메뉴 카테고리 "키즈 전문"(유아·초등 위주) 추가. `/kids/index.html` 신규
  생성 — `premium-tutoring.html`(특목·자사·과고 프리미엄 과외, 이 사이트에 이미
  있던 "기본 틀 + coming-box 준비중" 패턴이지만 실제로는 nav 어디에서도 링크 안
  되고 있던 고아 페이지였음)을 템플릿으로 재사용. 사용자가 아직 구체적 내용(연령대별
  가격, 실제 커리큘럼, 선생님 프로필)을 안 줘서, "키즈 전문이란" 섹션은 일반적인
  마케팅 카피로 채우고 "커리큘럼 안내" 섹션은 coming-box로 준비중 처리. 상담 CTA는
  자체 폼 없이 홈페이지 기존 `/#apply` 무료 수업 신청 폼으로 연결(중복 폼 안 만듦).
  sitemap.xml에도 등록.
- "외국어 전문(영어/일본어/중국어)"은 이번엔 보류 — 사용자가 키즈 전문만 먼저
  진행하기로 함. 나중에 만들 때는 이번 kids/ 패턴 그대로 재사용하면 됨.
- nav 반영 범위는 홈페이지(index.html)와 새로 만든 kids/index.html에만 한정.
  사이트 전체(수백 개 파일) nav에는 아직 "키즈 전문"이 안 보임 — 필요하면 전체
  파일 일괄 치환으로 별도 진행할 것.
- 홈페이지 nav를 손보는 김에, 이미 잠정 중단된 "대입컨설팅" 드롭다운(화상입시컨설팅/
  진학컨설팅/대치동컨설팅 하위 앵커)도 같은 파일에서 정리해 단순 링크로 바꿈 —
  consulting/index.html 쪽은 지난 세션에 이미 정리했었는데 홈페이지 쪽은 그대로
  남아있던 것을 이번에 맞춤.

### 2026-07-18 (3)
- 교육부의 「초·중등교육법」 개정(학교생활기록의 상업적 이용 제한) 관련 공지 이미지를
  사용자가 공유, 대입컨설팅(화상입시컨설팅/진학컨설팅/대치동컨설팅 — 학생부 관리·
  분석 포함)을 잠정 중단하기로 함. `consulting/index.html`을 기존 서비스 소개+신청폼
  페이지에서 "서비스 일시 중단 안내" 공지 페이지로 전면 교체(662줄→31줄, 상담 폼·
  구글시트 연동 스크립트·다음 우편번호 API 등 전부 제거). 과외 서비스는 이 방침과
  무관하므로 정상 운영 안내, CTA는 "1:1 맞춤 과외 문의하기"로 연결.
- `consulting/jinah.html`·`premium.html`·`video.html`은 사이트 어디서도 링크되지
  않는 고아 파일(전체 grep으로 확인, index.html에 앵커(#video/#jinah/#daechidong)로
  통합된 지 오래됨)이라 손대지 않음.
- 사이트 전체 페이지의 nav 드롭다운(대입컨설팅 하위 메뉴: 화상입시컨설팅/진학컨설팅/
  대치동컨설팅 앵커 링크)은 그대로 둠 — nav가 모든 페이지에 하드코딩 복사되어 있어
  전체 수정은 범위가 너무 큼. `/consulting/#video` 같은 링크를 눌러도 존재하지 않는
  앵커라 그냥 `/consulting/` 최상단(=중단 공지)으로 이동하므로 깨지지는 않음. 완전한
  정리가 필요하면(nav 하위 메뉴 자체를 지우거나 문구 변경) 별도 세션에서 전체 파일
  일괄 치환으로 진행할 것.

### 2026-07-18 (2)
- 고등학교용 블로그 콘텐츠를 5개 구 추가 제작: 성북·중랑·서대문·마포·중구. 사용자가
  동네별 고등학교 목록(구글독스/텍스트로 붙여넣기)을 줘서, 기존 도봉/노원/강북/은평/
  종로 5개 구 패턴(허브+동네+학교 3단 구조)을 그대로 재사용해 총 82개 페이지 생성
  (허브 5 + 동네 32 + 학교 45). 매번 처음부터 HTML을 손으로 쓰는 대신, 기존 페이지
  3종(hs-gwaoe 허브/동네/학교)을 템플릿으로 읽어서 Node.js 스크립트로 일괄 생성하는
  방식을 씀 — 스크립트는 스크래치 폴더에만 있고 저장소에는 없음(다음에 비슷한 작업
  필요하면 이 로그의 접근 방식 참고해서 다시 작성).
- 학교 슬러그 규칙: "OO고등학교"→"OOgo", "OO여자고등학교"→"OOyeoja"(go 없음),
  "OO과학/예술/디자인/외국어/국제/관광/미디어/IT고등학교"처럼 특수목적 유형 단어가
  있으면 "go" 생략(예: daeiloegugeo, hanseonggwahak), "OO대학교사범대학부속(여자)
  고등학교"는 "OOdaebusok(yeoja)"로 축약(예: goryeodaebusokgo, hongikdaebusokyeoja).
  기존 103개 슬러그와 겹치는 것 없음을 스크립트로 확인 후 생성.
- 동네 슬러그는 기존 중학교 동네 페이지(`{구}-{동}-gwaoe.html`)와 최대한 동일하게
  맞춤(예: 성북구 안암동→anamdong2ga, 삼선동→samseondong2ga — 실제 법정동 표기
  기준). 중학교 페이지가 없던 새 동네(서대문구 현저동→hyeonjeodong, 중구 만리동2가/
  장충동2가/예장동)는 새로 슬러그를 만듦.
- index.html·blog/index.html의 해당 5개 구에 고등학교 카드 추가(중학교 카드 옆에
  고등학교: [...] 블록 추가하는 기존 패턴 그대로). 썸네일은 `blog/images/서울고등/
  {구}.jpg`를 사용 — 이 폴더에 이미 25개 구 전부의 이미지가 준비되어 있었음(이전
  세션 기록엔 5개 구뿐인 줄 알았으나 실제로는 전체 25개 구 이미지가 다 있었음,
  다음에 새 구 고등학교 콘텐츠 만들 때는 이미지가 이미 있다고 가정하고 시작할 것).
- sitemap.xml에 82개 URL 전부 등록(허브 priority 0.8, 동네 0.7, 학교 0.6 — 기존
  고등학교 URL과 동일한 우선순위 체계).
- 남은 20개 구(강남·강동·강서·관악·광진·구로·금천·동대문·동작·서초·성동·송파·
  양천·영등포·용산 등)는 아직 고등학교 콘텐츠 없음 — 사용자가 학교 목록을 주면
  동일한 방식으로 이어서 만들면 됨.

### 2026-07-18
- 중학교 지역 카드/허브 페이지 제목에 "중학생" 표기 추가: "OO구 1:1 맞춤 과외 추천"
  → "OO구 중학생 1:1 맞춤 과외 추천". index.html·blog/index.html의 중학교 카드
  25개(alt/thumbTitle/title) + 25개 구 허브 페이지(`{구}-gwaoe.html`)의
  title 태그·meta description·h1·이미지 alt·하단 CTA까지 전부 반영. 이전에는
  고등학교 카드만 "고등학생"이 붙어 있어서 비대칭이었던 것을 통일함.
- 오래된 `C:\Users\ltw52\Projects\과외홈페이지` 폴더 발견 후 삭제. 이 폴더는
  git 커밋이 `479c5c9`(한참 이전)에 멈춰 있었고, 커밋 안 된 로컬 변경사항(이미지
  삭제, `.jpg.jpg` 중복 확장자 파일 등)도 쌓여 있었음 — 사용자 확인 후 폴더 전체
  삭제(웹사이트에는 영향 없음, 실제 작업 폴더는 이 OneDrive 폴더가 맞음).
- OneDrive 폴더 루트에 있던 `⚠️이폴더는_더이상_사용안함.txt`(2026-07-09 작성,
  Projects 폴더를 쓰라는 이제는 틀린 안내)도 삭제.
- `blog/goryeodae-susi-jeonhyeong.html`(고려대 수시전형 블로그 글)이 커밋 안 된
  채 남아있음 — 사용자가 아직 미완성이라고 확인, 커밋/sitemap 등록 보류 중.
  다음 세션에서 완성 여부 확인 후 진행.
- 지역 카드 이미지 전면 교체: 사용자가 OneDrive를 통해 `blog/images/서울중등전문`,
  `서울고등전문` 폴더를 삭제하고 새 이미지가 담긴 `서울중등`(번호 파일 26~50),
  `서울고등`(이미 구 이름으로 된 5개 구, 도봉·노원·강북·은평·종로) 폴더로 교체함.
  서울중등 번호 파일은 이미지에 박힌 구 이름 텍스트로 매핑을 확인 후(예: #26 =
  "중등전문 도봉구 맞춤과외") 25개 전부 구 이름으로 rename. 서울고등 폴더의
  `블로그배경..jpg`(점 두 개 오타)도 `블로그배경.jpg`로 수정. 이후 index.html·
  blog/index.html·모든 구 허브/동네/학교 페이지(63개 파일)에서 "서울중등전문"→
  "서울중등", "서울고등전문"→"서울고등" 경로를 전부 치환(전역 문자열 치환이라
  안전 — 두 폴더명이 서로 겹치지 않음). 고등학교는 애초에 이 5개 구만 콘텐츠가
  있어서 "5개 구만 바꿔달라"는 요청과 전체 치환 범위가 정확히 일치함.

### 2026-07-17 (데스크톱)
- 고등학교용 블로그 콘텐츠 신규 제작: 도봉구·노원구·강북구·은평구·종로구 5개 구에
  대해 허브(`{구}-hs-gwaoe.html`) + 동네별(`{구}-{동}-hs-gwaoe.html`) + 학교별
  (`{학교슬러그}-hs-gwaoe.html`) 3단 구조를 중학교 패턴과 동일하게 적용, 총 103개
  페이지 생성 (허브 5 + 동네 28 + 학교 70). 사용자가 준 동네별 고교 목록을 그대로
  사용했고(실제 학군 배정과 다를 수 있다는 안내 문구 포함), 학교 정보 placeholder
  박스도 중학교 페이지와 동일하게 넣음(실제 정보 없음).
- 고등학교는 수학/영어/국어 과목별 상세 페이지가 아직 없어서, 허브 페이지의
  과목별 카드는 3과목 모두 "준비 중"으로 표시(중학교처럼 수학만 콘텐츠 있는 게
  아니라 전부 미완성 상태). 추후 `{구}-hs-math-gwaoe.html` 같은 과목 전문 페이지를
  만들면 이 부분부터 연결하면 됨.
- 노원고등학교("nowon"), 은평고등학교("eunpyeong")는 슬러그가 구 이름과 겹쳐서
  각각 "nowongo", "eunpyeonggo"로 조정함 — 고등학교 슬러그를 새로 추가할 때 구
  슬러그와 겹치지 않는지 항상 확인할 것.
- blog/index.html, index.html의 districtPosts에 5개 구 모두 고등학교 항목 추가,
  sitemap.xml에 신규 103개 URL 전부 등록. 생성 스크립트는 이번에도 스크래치
  폴더에만 작성하고 저장소에는 커밋 안 함(재사용하려면 이 로그의 접근 방식 참고해서
  다시 작성).
- 아직 안 한 것: git add/commit/push (사용자 확인 후 진행 예정), 나머지 20개 구
  고등학교 콘텐츠(사용자가 준 데이터는 이 5개 구뿐).

### 2026-07-17 (노트북)
- 서울고등전문 썸네일 25장을 서울중등전문과 동일한 번호→구 매핑으로 이름 정리 완료,
  git에 추가 (이전에는 Projects 폴더에만 있고 커밋된 적 없었음)
- 고등학교용 블로그 콘텐츠(허브+동네+학교 페이지) 신규 제작은 아직 시작 전 —
  실제 고등학교 데이터(구/학군별 고교 목록) 필요, 다음 세션에서 사용자와 방식 논의 예정
- CLAUDE.md 신규 생성 (이 파일)

### 2026-07-17 (이전 세션들)
- 도봉구를 시범으로 중립 허브 페이지 패턴 설계 후, 나머지 24개 구 전체에 동일 패턴 적용
- 학교 페이지 391곳에 "학교 정보" placeholder 박스 추가
- sitemap.xml을 41개 → 641개 URL로 전면 재생성 (동네·학교별 페이지가 전혀 등록 안
  되어 있던 것을 발견하고 수정 — 네이버 검색 노출 문제의 핵심 원인이었음)
- 중학교 지역 카드 사진 25장을 "OO구 수학과외" → "OO구 맞춤과외" 문구로 교체
- 홈페이지(index.html)에 누락되어 있던 5개 구(서초/강동/금천/양천/영등포) 카드 추가
